[package]
name = "volf-native"
version = "1.0.4"
edition = "2021"
description = "Node/TypeScript binding for the Volf kernel sandbox"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", default-features = false, features = ["napi8"] }
napi-derive = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
volf-core = { path = "../../.." }

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
codegen-units = 1
